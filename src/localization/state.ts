let presentationLocale = 'en';

export function getPresentationLocale(): string {
  return presentationLocale;
}

export function setPresentationLocale(locale: string) {
  presentationLocale = locale;
}
